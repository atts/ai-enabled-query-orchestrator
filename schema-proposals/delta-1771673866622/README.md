# Schema Extension Proposal `delta-1771673866622`

**Auto-generated by the AI discovery service** on 2026-02-21T11:37:46.622Z

A user's natural language search requested product attributes that don't exist
in the current schema. This PR contains everything needed to add them.

## Proposed new fields

| Field | Type | Nullable |
|-------|------|----------|
| `sustainabilityScore` | `String` | Yes |
| `carbonFootprintKg` | `String` | Yes |
| `energyEfficiencyRating` | `String` | Yes |
| `recycledMaterialsPct` | `String` | Yes |

## Why these fields were requested

- **`sustainabilityScore`**: Requested via aiSearch prompt: "show me eco-friendly electronics with carbon footprint data"
- **`carbonFootprintKg`**: Requested via aiSearch prompt: "show me eco-friendly electronics with carbon footprint data"
- **`energyEfficiencyRating`**: Requested via aiSearch prompt: "show me eco-friendly electronics with carbon footprint data"
- **`recycledMaterialsPct`**: Requested via aiSearch prompt: "show me eco-friendly electronics with carbon footprint data"

## How to implement

1. Open `apps/products/src/main.ts`
2. In the SDL string, extend the `Product` type with the fields from `product-extension.graphql`
3. Copy the resolver stubs from `Product.resolver.stub.ts` into the `Product` resolver object
4. Replace each `return null` with real data-fetching logic
5. Merge this PR — the gateway will automatically expose the new fields

Once merged, future `aiSearch` queries requesting these concepts will return real data
instead of triggering another schema proposal.
