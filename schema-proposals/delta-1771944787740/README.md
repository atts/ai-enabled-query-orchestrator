# Schema Extension Proposal `delta-1771944787740`

**Auto-generated by the AI discovery service** on 2026-02-24T14:53:07.740Z

A user's natural language search requested product attributes that don't exist
in the current schema. This PR contains everything needed to add them.

## Proposed new fields

| Field | Type | Nullable |
|-------|------|----------|
| `sustainabilityScore` | `String` | Yes |
| `ecoFriendlyRating` | `String` | Yes |
| `carbonFootprint` | `String` | Yes |
| `recyclableMaterials` | `String` | Yes |
| `organicCertification` | `String` | Yes |
| `energyEfficiencyRating` | `String` | Yes |
| `environmentalImpactScore` | `String` | Yes |
| `sustainabilityCertifications` | `String` | Yes |
| `biodegradable` | `String` | Yes |
| `renewableMaterialsPercentage` | `String` | Yes |

## Why these fields were requested

- **`sustainabilityScore`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`ecoFriendlyRating`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`carbonFootprint`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`recyclableMaterials`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`organicCertification`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`energyEfficiencyRating`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`environmentalImpactScore`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`sustainabilityCertifications`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`biodegradable`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"
- **`renewableMaterialsPercentage`**: Requested via aiSearch prompt: "show me eco-friendly sustainable products"

## How to implement

1. Open `apps/products/src/main.ts`
2. In the SDL string, extend the `Product` type with the fields from `product-extension.graphql`
3. Copy the resolver stubs from `Product.resolver.stub.ts` into the `Product` resolver object
4. Replace each `return null` with real data-fetching logic
5. Merge this PR — the gateway will automatically expose the new fields

Once merged, future `aiSearch` queries requesting these concepts will return real data
instead of triggering another schema proposal.
